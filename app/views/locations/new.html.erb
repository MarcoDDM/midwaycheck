<div class="container">
  <h1>Midway Check</h1>
  <center>
    <h2>Encuentra el mejor punto intermedio</h2>
  </center>
  <p class="catchy-phrase">Si quieres reunirte con un amigo o una cita, elige el punto medio perfecto. <br>Te damos opciones como cafeterías, cines, shoppings, e incluso parques y plazas si no quieres gastar dinero.</p>
  <br>
  <br>
  <br>
  <%= form_with url: locations_path, method: :post, id: 'midway-form' do |form| %>
    <div class="form-group">
      <%= form.label :address1, 'Dirección o ciudad 1' %>
      <%= form.text_field :address1, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= form.label :address2, 'Dirección o ciudad 2' %>
      <%= form.text_field :address2, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= form.label :results_limit, 'Mostrar resultados' %>
      <%= form.select :results_limit, { 'Mejor' => 'best', '5 Mejores' => 'top5', 'Todos' => 'all' }, { prompt: 'Seleccionar' }, class: 'form-control' %>
    </div>
    <%= form.submit 'Buscar', class: 'btn btn-primary' %>
  <% end %>
</div>

<div class="results" id="results-container">
  <ul id="results-list">
    <!-- Aquí se mostrarán dinámicamente los resultados -->
  </ul>
</div>
